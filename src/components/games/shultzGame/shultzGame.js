import React, {useState} from 'react';
import './shultzGame.scss'
import {connect} from "react-redux";

let indexGame = 0;
let gamesClosedNames = [];
function ShultzGame(props) {
    const {gameName, game} = props;
    return (

    );
}

export default connect()(ShultzGame);
